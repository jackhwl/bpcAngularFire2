import { Injectable } from '@angular/core';

export class Env {
    html5Mode: boolean;
    hostname: string;
    apiUrl: string;
    host: string;
    enableDebug: boolean;
    enableSSL: boolean;
    autoResolveHost: boolean;
}